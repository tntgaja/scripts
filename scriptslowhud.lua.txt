loadstring(game:HttpGet(("https://raw.githubusercontent.com/daucobonhi/Ui-Redz-V2/refs/heads/main/UiREDzV2.lua")))()

       local Window = MakeWindow({
         Hub = {
         Title = "TNT Roblox",
         Animation = "By: TNTgj Roblox"
         },
        Key = {
        KeySystem = false,
        Title = "Key System",
        Description = "",
        KeyLink = "",
        Keys = {"1234"},
        Notifi = {
        Notifications = true,
        CorrectKey = "Running the Script...",
       Incorrectkey = "The key is incorrect",
       CopyKeyLink = "Copied to Clipboard"
      }
    }
  })

       MinimizeButton({
       Image = "http://www.roblox.com/asset/?id=83190276951914",
       Size = {60, 60},
       Color = Color3.fromRGB(10, 10, 10),
       Corner = true,
       Stroke = false,
       StrokeColor = Color3.fromRGB(255, 0, 0)
      })
      
------ Tab
local Tab1o = MakeTab({Name = "Auto farm"})
local Tab2o = MakeTab({Name = "combat"})

------ BUTTON
-- local autofarm = false

-- Hàm tìm NPC nhiệm vụ Bandit
local function GetQuestNPC()
    for _, npc in pairs(workspace.NPCs:GetChildren()) do
        if npc.Name == "QuestBandit" then
            return npc
        end
    end
    return nil
end

-- Hàm nhận nhiệm vụ Bandit
local function TakeQuest()
    local npc = GetQuestNPC()
    if npc and npc:FindFirstChild("HumanoidRootPart") then
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = npc.HumanoidRootPart.CFrame + Vector3.new(0,3,0)
        task.wait(0.5)
        game.ReplicatedStorage.Remotes.Quest:FireServer("BanditQuest")
    end
end

-- Hàm tìm Bandit còn sống
local function GetBandit()
    for _, mob in pairs(workspace.Enemies:GetChildren()) do
        if mob.Name == "Bandit" and mob:FindFirstChild("Humanoid") and mob.Humanoid.Health > 0 then
            return mob
        end
    end
    return nil
end

-- Hàm đánh Bandit
local function AttackBandit(bandit)
    while bandit and bandit:FindFirstChild("Humanoid") and bandit.Humanoid.Health > 0 and autofarm do
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = bandit.HumanoidRootPart.CFrame + Vector3.new(0,0,2)
        game.ReplicatedStorage.Remotes.Attack:FireServer(bandit)
        task.wait(0.3)
    end
end

-- Toggle nút AutoFarm Bandit
AddToggle(Tab1o, {
    Name = "Auto Farm Bandit",
    Default = false,
    Callback = function(Value)
        autofarm = Value
        if autofarm then
            spawn(function()
                while autofarm do
                    local bandit = GetBandit()
                    if not bandit then
                        -- nếu chưa có quái thì nhận nhiệm vụ lại
                        TakeQuest()
                    else
                        AttackBandit(bandit)
                    end
                    task.wait(0.5)
                end
            end)
        end
    end
})
  AddButton(Tab2o, {
     Name = "blue x",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/realredz/BloxFruits/refs/heads/main/Source.lua"))(Settings)
  end
  })